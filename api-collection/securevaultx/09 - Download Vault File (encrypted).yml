info:
  name: 09 - Download Vault File (encrypted)
  type: http
  seq: 9

http:
  method: GET
  url: "{{baseUrl}}/api/vault/files/PASTE_FILE_ID_HERE/encrypted"

docs: >
  Streams the stored .enc file exactly as it sits on disk (still
  encrypted) — useful to inspect the SVXF header with a hex viewer.

settings:
  encodeUrl: true
